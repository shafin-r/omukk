import React from "react";
import { motion } from "framer-motion";

const HoverBar = () => {
  return <motion.div className="h-0.5 bg-black rounded-full w-0"></motion.div>;
};

export default HoverBar;
